{
  "name": "wanxiang-card",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -R index.html styles.css app.js sync-config.js manifest.webmanifest sw.js assets dist/"
  }
}
